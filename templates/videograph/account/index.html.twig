{% extends template ~ '/base.html.twig' %}

{% block title %}Page logged{% endblock %}

{% block body %}
<!-- Breadcrumb Begin -->                     
    <div class="breadcrumb-option spad set-bg" data-setbg={{ asset(template ~ '/img/breadcrumb-bg.jpg') }}>
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <div class="breadcrumb__text">
                        <h2>Bienvenue {{ app.user.username }}</h2>
                        <div class="breadcrumb__links">
                            <a href="{{ path('home') }}">Home</a>
                            <span>Logged</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Breadcrumb End -->
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-
    if; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper code">
    <h1>Hello {{ controller_name }}! ✅</h1>
   

    {% if app.user %}
        <div class="mb-3">
            Login réussi,bienvenue {{ app.user.pseudo }} \\//{{ app.user.username }}✅, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'E:/ALAJI/Stage/src/Controller/AccountController.php'|file_link(0) }}">src/Controller/AccountController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'E:/ALAJI/Stage/templates/account/index.html.twig'|file_link(0) }}">templates/account/index.html.twig</a></code></li>
    </ul>
</div>


{% endblock %}
